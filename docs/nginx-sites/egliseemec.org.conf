server {
    listen 80;
    server_name egliseemec.org www.egliseemec.org;

    root /home/projects/emec-projects/emec/dist;
    index index.html;

    access_log /var/log/nginx/egliseemec.org.access.log;
    error_log /var/log/nginx/egliseemec.org.error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
