server {
    listen 80;
    server_name dosc.egliseemec.org;

    root /home/projects/emec-projects/messages-emec/dist;
    index index.html;

    access_log /var/log/nginx/dosc.egliseemec.org.access.log;
    error_log /var/log/nginx/dosc.egliseemec.org.error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
